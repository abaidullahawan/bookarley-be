<style>
  /* Style for the carousel control icons */
.carousel-control-icon {
  width: 40px;
  height: 40px;
  display: flex;
  justify-content: center;
  align-items: center;
}

.carousel-control-icon i {
  font-size: 1.5rem;
}


/* Style for the icon color */
.carousel-control-icon i {
  color: white; /* Change the icon color as needed */
}

:root {
  --visible-slides: 5;
}


.slider {
  position: relative;
  height: 200px;
  overflow: hidden;
}

.vendor-slider {
  height: 237px !important;
}


.slider-ul {
  position: relative;
  list-style: none;
  margin: 0;
  padding-left: 70px;
  padding-right: 70px;
  width: 100%;
  height: 100%;
  display: flex;
  transform: translateX(calc(-100% / var(--visible-slides)));
}

.vendor-ul {
  position: relative;
  list-style: none;
  margin: 0;
  padding-left: 198px;
  padding-right: 42px;
  width: 100%;
  height: 100%;
  display: flex;
  transform: translateX(calc(-100% / var(--visible-slides)));
}

.slider-li {
  position: relative;
  width: calc(92% / var(--visible-slides));
  height: 100%;
  color: black;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-shrink: 0;
  background: white;
}

@media(max-width: 992px){
  .slider-li {
    width: calc(182% / var(--visible-slides)) !important;
  }
}

@media(max-width: 992px){
  .slider-li {
    width: calc(233% / var(--visible-slides)) !important;
  }
  .vendor-ul {
    padding-left: 120px !important;
    padding-right: 130px !important;
  }
}

@media(max-width: 320px){
  .slider-li {
    width: calc(233% / var(--visible-slides)) !important;
  }
  .vendor-ul {
    padding-left: 80px !important;
    padding-right: 130px !important;
  }
  .vendor-li {
    width: calc(1304% / var(--visible-slides)) !important;
  }
}


@media(max-width: 768px){
  .slider-li {
    width: calc(260% / var(--visible-slides)) !important;
  }
}

@media(max-width: 430px){
  .slider-li {
    width: calc(415% / var(--visible-slides)) !important;
  }
}

/* Existing CSS */

.vendor-li {
  margin-left: 5%;
  position: relative;
  width: calc(188% / var(--visible-slides));
  height: 100%;
  color: black;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-shrink: 0;
  background: white;
  display: flex !important;
  flex-direction: column; /* Update to column layout */
  justify-content: flex-start; /* Align items to start in column layout */
}

/* Updated styles */

.vendor-li .image-section {
  width: 100%;
  height:75%; /* Divide the li into two equal parts vertically */
  overflow: hidden;
}

.vendor-li .image-section img {
  object-fit: cover;
  width: 100%;
  height: 100%;
}

.vendor-li .text-section {
  width: 100%;
  height: 25%; /* Divide the li into two equal parts vertically */
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 1.5em;
  font-weight: 400;
}



.slider-nav .prev,
.slider-nav .next {
  position: absolute;
  background: #1e396b;
  height: 32px;
  width: 32px;
  top: 52%;
  transform: translateY(-50%);
  border-radius: 50%;
  cursor: pointer;
}

.slider-nav .prev {
  left: 0px;
}

.slider-nav .next {
  right: 0px;
}

.transition {
  transition: transform 0.5s ease;
}

.left {
  transform: translateX(-100%);
}

.right {
  transform: translateX(100%);
}

/*--------------------------------------------------------------
# Values
--------------------------------------------------------------*/
.values .box {
  padding: 30px;
  box-shadow: 0px 0 32px rgb(1 41 112 / 8%);
  text-align: center;
  transition: 0.3s;
  height: 100%;
  border: 0.5px solid #e2e1e1;
  border-radius: 10px;
}


.values .box h3 {
  font-size: 22px;
  color: #012970;
  font-weight: 700;
  margin-bottom: 18px;
  margin-top: 35px;
}

.values .box:hover {
  box-shadow: 0px 0 30px rgba(1, 41, 112, 0.08);
}


.section-header {
  text-align: center;
  padding-bottom: -2px;
}

.section-header h2 {
  font-size: 13px;
  letter-spacing: 1px;
  font-weight: 700;
  margin: 0;
  color: #f29f37;
  text-transform: uppercase;
}

.section-header p {
  margin: 10px 0 0 0;
  padding: 0;
  font-size: 24px;
  line-height: 42px;
  font-weight: 700;
  color: #012970;
}

@media (max-width: 768px) {
  .section-header p {
    font-size: 28px;
    line-height: 32px;
  }
}

.section-title {
  text-align: center;
  padding-bottom: 30px;
}

.section-title h2 {
  font-size: 28px;
  font-weight: bold;
  text-transform: uppercase;
  margin-bottom: 20px;
  padding-bottom: 20px;
  position: relative;
  color: #37517e;
}

.section-title h2::before {
  content: "";
  position: absolute;
  display: block;
  width: 120px;
  height: 1px;
  background: #ddd;
  bottom: 1px;
  left: calc(50% - 60px);
}

.section-title h2::after {
  content: "";
  position: absolute;
  display: block;
  width: 40px;
  height: 3px;
  background: #1e396b;
  bottom: 0;
  left: calc(50% - 20px);
}

.section-title p {
  margin-bottom: 0;
  font-size: 1.4em;
  color: #635b5b;
}
.vendors-container{
  margin-top: 10%;
}

.feature-text{
  font-size: 1.3em;
  width: 150px;
  padding: 9px;
  background: #1e396b;
  color: white;
  font-weight: 400;
  border-radius: 32px;
  margin-bottom: 24px;
  text-align: center;
}

/*--------------------------------------------------------------
# About Us
--------------------------------------------------------------*/

#about{
  background: #f3f5fa;
  padding-top: 60px;
  padding-bottom: 60px;
  margin-top: 80px;
}

.about .content h3 {
  font-weight: 600;
  font-size: 26px;
}

.about .content ul {
  list-style: none;
  padding: 0;
}

.about .content ul li {
  padding-left: 28px;
  position: relative;
}

.about .content ul li+li {
  margin-top: 10px;
}

.about .content ul i {
  position: absolute;
  left: 0;
  top: 2px;
  font-size: 20px;
  color: #47b2e4;
  line-height: 1;
}

.about .content p:last-child {
  margin-bottom: 0;
}

.about .content .btn-learn-more {
  font-family: "Poppins", sans-serif;
  font-weight: 500;
  font-size: 14px;
  letter-spacing: 1px;
  display: inline-block;
  padding: 12px 32px;
  border-radius: 4px;
  transition: 0.3s;
  line-height: 1;
  color: #1e396b;
  -webkit-animation-delay: 0.8s;
  animation-delay: 0.8s;
  margin-top: 6px;
  border: 2px solid #1e396b;
  text-decoration: none;
  border-radius: 30px !important;
}

  .about .content .btn-learn-more:hover {
    background: #1e396b;
    color: #fff;
    text-decoration: none;
  }

  .box p{
    font-size: 1.2em;
    color: slategray;
  }


  /* unique venue card style  */
  .Unique-Venues-container{
    margin-top: 5%;
    margin-bottom: 5%;
  }

 .image-container {
    overflow: hidden;
    position: relative;
    width: 100%;
  }

 .image-container img {
    height: 200px;
    object-fit: cover;
    transition: transform 0.3s ease-in-out;
    width: 100%;
  }

  .image-container:hover img {
    transform: scale(1.1); /* Zoom effect on hover */
  }

  @media screen and (max-width: 767px){
    .image-container img {
      width: 100%;
    }

    .vendor-li {
      width: calc(427% / var(--visible-slides));
    }
  }
  @media screen and (max-width: 575px){
    .unique-vanue-card {
      margin: 0px 15px;
    }

    .vender-cards .col-lg-4 {
      padding: 0px 20px;
    }
    .static-text-card{
      padding: 20px 20px 0px 20px;
    }
    .section-title {
      padding-left: 20px;
      padding-right: 20px;
    }
  }

  @media (max-width: 499px){

    .vendor-ul {
        padding-left: 80px !important;
        padding-right: 80px !important;
    }
    .vendor-slider {
        margin: 0px 20px 0px 0px;
    }
  }

  .unique-vanue-card {
    position: relative;
    box-shadow: 0px 0 32px rgb(1 41 112 / 8%);
    border: 0.5px solid #e2e1e1;
    height: 100%;
  }
  .unique-vanue-card a{
    text-decoration: none;
  }

  .unique-vanue-tumb {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0; 
    background: white;
    overflow: hidden;
  }

  .unique-vanue-des {
    padding: 20px;
    background-color: white;
  }

  .unique-vanue-des h4 {
    text-align: center;
    color: black;
    padding-bottom: 10px;
  }
  .unique-vanue-des p {
    font-size: 15px;
    line-height: 22px;
    color: black;
  }

  .book-now-button{
    position: absolute;
    bottom: 1%;
    left: 24%;
    background: #f29f37;
    border: none;
    color: #1e396b;
    font-size: 1.3em;
    font-weight: 500;
    border-radius: 15px;
    padding: 5px 25px;
  }


</style>

<div class="mt-5">
   <div id="myCarousel" class="carousel slide" data-bs-ride="carousel">
    <!-- Slides -->
    <% if Spree::Slider.all.count > 0 %>
      <div class="carousel-inner">
        <% Spree::Slider.all.each_with_index do |slider, index| %>
          <div class="carousel-item <%= index == 0 ? 'active' : '' %>">
            <%= image_tag(slider.image, alt: slider.title, class: 'd-block w-100') %>
            <a href="<%= slider.url %>" class="slider-button book-now-button btn btn-secondary shadow-sm">Book Now</a>
          </div>
        <% end %>

      </div>
    <% else %>
      <div class="carousel-inner">
        <div class="carousel-item active">
          <%= image_tag("banner-1-upd.jpg", alt:"Image 1", class:"d-block w-100") %>
        </div>
      </div>
    <% end %>

    <!-- Left and right controls -->
    <a class="carousel-control-prev" href="#myCarousel" data-bs-slide="prev">
      <span class="carousel-control-prev-icon"></span>
    </a>
    <a class="carousel-control-next" href="#myCarousel" data-bs-slide="next">
      <span class="carousel-control-next-icon"></span>
    </a>
  </div>
</div>

<div class="container" style="margin-top: 1%; margin-bottom: 2%;">
	<div>
		<div class="slider">
			<ul class="slider-ul">
				<% Spree::Taxonomy.all.each do |taxonomy| %>
					<% taxonomy&.root&.children&.each do |parent_taxon| %>
						<% parent_taxon&.children&.each do |child_taxon| %>
              <% child_taxon_permalink = child_taxon&.permalink %>
                <li class="slider-li d-grid">
                  <%= link_to "/t/#{child_taxon_permalink}", style:"text-decoration: none;" do %>
                    <div class="d-flex justify-content-center">
                      <% if child_taxon&.icon&.attached? %>
                        <%= render(ImageComponent.new(image: child_taxon.icon, size: :small, itemprop: "image", style: "width: 85px;")) %>
                      <% end %>
                    </div>
                    <div class="d-flex justify-content-center text-dark" style="font-size: 1.3em;font-weight: 500;">  
                      <%= child_taxon&.name %>
                    </div>
                  <% end %>
                </li>
				    <% end %>
				  <% end %>
				<% end %>
			</ul>
      <div class="slider-nav">
        <div class="prev d-flex justify-content-center align-items-center"><i class="fa fa-arrow-left fa-2x text-light"></i></div>
        <div class="next d-flex justify-content-center align-items-center"><i class="fa fa-arrow-right fa-2x text-light"></i></div>
      </div>
		</div>
	</div>
</div>
<hr style="color: #b5b1b1;">

<section id="values" class="values" style="margin-top: 80px">
  <div class="container" data-aos="fade-up">
    <div class="d-flex justify-content-center">
      <h2 class="feature-text">Bookarley Features</h2>
    </div>  
    <header class="section-header">
      <div class="section-title">
        <h2>Book Venues and Vendors Effortlessly</h2>
      </div>
    </header>

    <div class="row vender-cards">
      <div class="col-lg-4" data-aos="fade-up" data-aos-delay="200">
        <div class="box">
          <%= image_tag("search.png", alt: "Logo", width: 100) %>
          <h3>Browse Categories</h3>
          <p>Check out the best suited Venues & Vendors, compare photos, special offers and function packages.</p>
        </div>
      </div >

      <div class="col-lg-4 mt-4 mt-lg-0" data-aos="fade-up" data-aos-delay="400">
        <div class="box">
          <%= image_tag("quote.png", alt: "Logo", width: 145) %>
          <h3>Request Quotes</h3>
          <p>Get custom quotes of your short-listed Venues at the click of GET FREE QUOTES button.</p>
        </div>
      </div >

      <div class="col-lg-4 mt-4 mt-lg-0" data-aos="fade-up" data-aos-delay="600">
        <div class="box">
          <%= image_tag("book.png", alt: "Logo", width: 100) %>
          <h3>Book your Choice</h3>
          <p>Unlock Inspiring Spaces: Your Event, Your Unique Venue, Your Way to Make Every Moment Special.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ======= About Us Section ======= -->
<section id="about" class="about">
  <div class="container" data-aos="fade-up">
    <div class="row content">
      <div class="col-lg-6">
        <div class="d-flex justify-content-center align-items-center">
          <%= image_tag("venue-back.png", alt: "Logo", style:"width: 100%;") %>
        </div>
      </div>
      <div class="col-lg-6 pt-4 pt-lg-0 d-flex justify-content-center align-items-center static-text-card">
        <div>
          <h1 class="mb-4" style="color: #1e396b; font-weight: 550; font-size: 30px;">Events & Wedding Venues across Pakistan</h1>
          <p style="    
            font-size: 1.6em;
            color: darkslategray;"
          >
            Exploring Remarkable Venues. Discover Your Perfect Space. Start Your Adventure!
          </p>
          <a href="t/venues" class="btn-learn-more mt-4">Explore All Venues</a>
        </div>  
      </div>
    </div>
  </div>
</section><!-- End About Us Section -->

<div class="container vendors-container">
  <div class="section-title">
    <h2>Vendors by Categories</h2>
    <p>Elevate your celebrations with our premium event rental services. From fairy-tale weddings to milestone birthdays, we curate an ambiance that transforms moments into unforgettable memories, reflecting your unique style and charm.</p>
  </div>
  <div>
    <div class="slider vendor-slider">
      <ul class="vendor-ul">
        <% Spree::Taxonomy.where(name: "Vendors").each do |taxonomy| %>
          <% taxonomy.root.children.each do |parent_taxon| %>
            <% parent_taxon_permalink = parent_taxon&.permalink %>
            <li class="vendor-li card shadow-sm d-grid">
                <div class="image-section">
                  <%= link_to "/t/#{parent_taxon_permalink}", style:"text-decoration: none;" do %>
                    <% if parent_taxon&.icon&.attached? %>
                      <%= render(ImageComponent.new(image: parent_taxon.icon, size: :small, itemprop: "image", style: "width: 100%;")) %>
                    <% end %>
                  <% end %>
                </div>
                <%= link_to "/t/#{parent_taxon_permalink}", style:"text-decoration: none; margin-top: 25px; color: black; width: 100%;" do %>
                  <div class="text-section">
                    <%= parent_taxon.name %>
                  </div> 
                <% end %>
            </li>
          <% end %>
        <% end %>
      </ul>
      <div class="slider-nav">
        <div class="prev d-flex justify-content-center align-items-center"><i class="fa fa-arrow-left fa-2x text-light"></i></div>
        <div class="next d-flex justify-content-center align-items-center"><i class="fa fa-arrow-right fa-2x text-light"></i></div>
      </div>
    </div>
  </div>
</div>


<!-- ======= About Us Section ======= -->
<section id="about" class="about">
  <div class="container" data-aos="fade-up">
    <div class="row content">
      <div class="col-lg-6">
        <div class="d-flex justify-content-center align-items-center">
          <%= image_tag("invite_img.png", alt: "Logo", style:"width: 80%;") %>
        </div>
      </div>
      <div class="col-lg-6 pt-4 pt-lg-0 d-flex justify-content-center align-items-center static-text-card">
        <div>
          <h1 class="mb-4" style="color: #1e396b; font-weight: 550; font-size: 30px;">Design Your Digital Invitations</h1>
          <p style="    
            font-size: 1.6em;
            color: darkslategray;"
          >
            Bring Together Your Dear Ones to Mark Important Life Events!
          </p>
          <%= link_to invitation_cards_path, class:"btn-learn-more mt-4" do %>
            Start Your Journey
          <% end %>  
        </div>  
      </div>
    </div>
  </div>
</section><!-- End About Us Section -->

<%# unique vaneus section  %>
<div class="container Unique-Venues-container">
  <div class="section-title">
    <h2> Unique Venues</h2>
    <p>These carefully chosen spaces not only reflect the personality and style 
    of the celebrants but also provide an ambiance that elevates every moment, 
    making it a reflection of the significance and magic of the day.</p>
  </div>
  <%# card box  %>
<div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4" style="margin-bottom: 80px;">
  <% Spree::Taxonomy.where(name: "Venues").each do |taxonomy| %>
    <% taxonomy.root.children.where(name: "Unique Venues").each do |parent_taxon| %>
      <% parent_taxon.children.each do |child_taxon| %>
        <div class="col">

          <div class="unique-vanue-card">
           <% child_permalink = child_taxon&.permalink %>
            <%= link_to "/t/#{child_permalink}" do %>
              <div class="unique-vanue-tumb">
              
                <% if child_taxon&.icon&.attached? %>
                <div class="image-container">
                  <%= render(ImageComponent.new(image: child_taxon.icon, size: :small, itemprop: "image")) %>
                  </div>
                <% end %>
              </div>
              <div class="unique-vanue-des">
                <h4 class="d-flex justify-content-center " style="font-size: 1.9em; font-weight: 500;">
                  <%= child_taxon.name %>
                </h4>
                <div class="unique-v-bottom-details">
                  <p class="d-flex justify-content-center text-center" style="font-size: 1.3em; font-weight: 400;">
                    <%= child_taxon.description %>
                  </p>
                </div>
              </div>
            <%end%>
          </div>
        </div>
      <% end %>
    <% end %>
  <% end %>
</div>

</div>
<%# unique vaneus section end  %>


<script>
  $(document).ready(function () {
    function resetPos(slider) {
      slider.find('ul li').removeClass('transition');
      slider.find('ul li').removeClass('left right');
    }

    function prev(slider) {
      var sliderLength = slider.find('ul li').length;
      slider.find('ul li').each(function () {
        var curPos = parseInt($(this).css('order'));
        if (curPos < sliderLength) {
          $(this).css('order', curPos + 1);
        } else {
          curPos = 1;
          $(this).css('order', curPos);
        }
      });
    }

    function next(slider) {
      var sliderLength = slider.find('ul li').length;
      slider.find('ul li').each(function () {
        var curPos = parseInt($(this).css('order'));
        if (curPos > 1) {
          $(this).css('order', curPos - 1);
        } else {
          curPos = sliderLength;
          $(this).css('order', curPos);
        }
      });
    }

    $('.prev').on('click', function () {
      var slider = $(this).closest('.slider');
      slider.find('ul li').addClass('transition right');
      setTimeout(function () {
        prev(slider);
        resetPos(slider);
      }, 500);
    });

    $('.next').on('click', function () {
      var slider = $(this).closest('.slider');
      slider.find('ul li').addClass('transition left');
      setTimeout(function () {
        next(slider);
        resetPos(slider);
      }, 500);
    });

    function init(slider) {
      slider.find('ul li').each(function (index) {
        $(this).css('order', index + 1);
      });
    }

    $('.slider').each(function () {
      init($(this));
    });
  });
</script>